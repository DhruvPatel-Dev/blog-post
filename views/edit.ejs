<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>

    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
    <p>Edit page</p>
    <h1>Hello, <%= post.user.username %></h1>

    <h1 class="mb-5">Edit a post</h1>
    <form action="/edit/<%= post._id %>" method="post">
        <textarea name="content" class="bg-transparent border-2 border-zinc-800 block mb-3" placeholder=""><%= post.content %></textarea>
        <input class="bg-green-600 rounded-md block mb-3 px-3 py-2"  type="submit" value="edit Post">
        <a class="bg-red-600 rounded-md px-3 py-2" href="/logout">Logout</a>
    </form>




 
           
   </div>
    

</body>
</html>